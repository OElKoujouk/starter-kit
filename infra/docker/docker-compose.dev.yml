# Docker Compose pour le développement
# Usage: docker-compose -f infra/docker/docker-compose.dev.yml up -d

services:
  # =========================
  # BASE DE DONNÉES
  # =========================
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-starter_kit}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 5s
      timeout: 5s
      retries: 10

  # =========================
  # API (optionnel si dev en local)
  # =========================
  # api:
  #   build:
  #     context: ../..
  #     dockerfile: app/api/Dockerfile
  #   env_file:
  #     - ../../.env
  #   environment:
  #     DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@db:5432/${POSTGRES_DB:-starter_kit}
  #   depends_on:
  #     db:
  #       condition: service_healthy
  #   ports:
  #     - "${API_PORT:-4000}:4000"

  # =========================
  # CLIENT (optionnel si dev en local)
  # =========================
  # client:
  #   build:
  #     context: ../..
  #     dockerfile: app/client/Dockerfile
  #   env_file:
  #     - ../../.env
  #   depends_on:
  #     - api
  #   ports:
  #     - "${CLIENT_PORT:-3000}:3000"

volumes:
  db_data:
